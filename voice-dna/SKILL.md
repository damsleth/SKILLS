---
name: voice-dna
description: Backward-compatibility alias for `cj-voice-dna`. Use only when the user explicitly invokes `/skill:voice-dna`; otherwise use `cj-voice-dna`.
disable-model-invocation: true
---

# Alias: voice-dna -> cj-voice-dna

This skill name is kept as a backward-compatibility alias.

When this alias is invoked:

1. Immediately read `../cj-voice-dna/SKILL.md`.
2. Follow `cj-voice-dna` as the canonical implementation.
3. Treat `cj-voice-dna` as the authoritative skill name in any follow-up guidance or cross-skill references.
