---
name: calendar
description: Backward-compatibility alias for `cj-calendar`. Use only when the user explicitly invokes `/skill:calendar`; otherwise use `cj-calendar`.
disable-model-invocation: true
---

# Alias: calendar -> cj-calendar

This skill name is kept as a backward-compatibility alias.

When this alias is invoked:

1. Immediately read `../cj-calendar/SKILL.md`.
2. Follow `cj-calendar` as the canonical implementation.
3. Treat `cj-calendar` as the authoritative skill name in any follow-up guidance or cross-skill references.
