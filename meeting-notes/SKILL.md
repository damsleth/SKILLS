---
name: meeting-notes
description: Backward-compatibility alias for `cj-meeting-notes`. Use only when the user explicitly invokes `/skill:meeting-notes`; otherwise use `cj-meeting-notes`.
disable-model-invocation: true
---

# Alias: meeting-notes -> cj-meeting-notes

This skill name is kept as a backward-compatibility alias.

When this alias is invoked:

1. Immediately read `../cj-meeting-notes/SKILL.md`.
2. Follow `cj-meeting-notes` as the canonical implementation.
3. Treat `cj-meeting-notes` as the authoritative skill name in any follow-up guidance or cross-skill references.
