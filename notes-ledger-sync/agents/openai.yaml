interface:
  display_name: "Notes + Ledger Sync"
  short_description: "Create notes and sync cognitive ledger"
  default_prompt: "Use $notes-ledger-sync to turn my input into a note in $NOTES_DIR and synchronized memory updates in $LEDGER_DIR."
