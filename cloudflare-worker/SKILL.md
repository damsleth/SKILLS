---
name: cloudflare-worker
description: Backward-compatibility alias for `cj-cloudflare-worker`. Use only when the user explicitly invokes `/skill:cloudflare-worker`; otherwise use `cj-cloudflare-worker`.
disable-model-invocation: true
---

# Alias: cloudflare-worker -> cj-cloudflare-worker

This skill name is kept as a backward-compatibility alias.

When this alias is invoked:

1. Immediately read `../cj-cloudflare-worker/SKILL.md`.
2. Follow `cj-cloudflare-worker` as the canonical implementation.
3. Treat `cj-cloudflare-worker` as the authoritative skill name in any follow-up guidance or cross-skill references.
