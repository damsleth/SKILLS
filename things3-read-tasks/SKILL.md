---
name: things3-read-tasks
description: Backward-compatibility alias for `cj-things3-read-tasks`. Use only when the user explicitly invokes `/skill:things3-read-tasks`; otherwise use `cj-things3-read-tasks`.
disable-model-invocation: true
---

# Alias: things3-read-tasks -> cj-things3-read-tasks

This skill name is kept as a backward-compatibility alias.

When this alias is invoked:

1. Immediately read `../cj-things3-read-tasks/SKILL.md`.
2. Follow `cj-things3-read-tasks` as the canonical implementation.
3. Treat `cj-things3-read-tasks` as the authoritative skill name in any follow-up guidance or cross-skill references.
