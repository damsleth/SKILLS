---
name: did
description: Backward-compatibility alias for `cj-did`. Use only when the user explicitly invokes `/skill:did`; otherwise use `cj-did`.
disable-model-invocation: true
---

# Alias: did -> cj-did

This skill name is kept as a backward-compatibility alias.

When this alias is invoked:

1. Immediately read `../cj-did/SKILL.md`.
2. Follow `cj-did` as the canonical implementation.
3. Treat `cj-did` as the authoritative skill name in any follow-up guidance or cross-skill references.
