---
name: yaams
description: Backward-compatibility alias for `cj-yaams`. Use only when the user explicitly invokes `/skill:yaams`; otherwise use `cj-yaams`.
disable-model-invocation: true
---

# Alias: yaams -> cj-yaams

This skill name is kept as a backward-compatibility alias.

When this alias is invoked:

1. Immediately read `../cj-yaams/SKILL.md`.
2. Follow `cj-yaams` as the canonical implementation.
3. Treat `cj-yaams` as the authoritative skill name in any follow-up guidance or cross-skill references.
